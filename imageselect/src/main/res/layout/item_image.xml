<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#888888">


    <ImageView
        android:id="@+id/item_image_iv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:scaleType="centerInside"
        android:src="@mipmap/imageselector_photo"/>

    <TextView
        android:id="@+id/item_image_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:drawablePadding="4dp"
        android:drawableTop="@mipmap/imageselector_select_photo"
        android:gravity="center"
        android:text="拍照"
        android:textSize="14sp"
        />

    <ImageView
        android:id="@+id/item_image_select_iv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|right"
        android:contentDescription="@null"
        android:src="@mipmap/imageselector_select_uncheck"/>

</FrameLayout>